# Convex backend (Ivano PMS)

Nine-table hospitality schema: property, unit, guest, booking, bookingChannelMessage, manager, checklist, occupancySnapshot, auditLog.

Functions live under `convex/functions/`. Run `pnpm seed` after `npx convex dev` to load demo data.
